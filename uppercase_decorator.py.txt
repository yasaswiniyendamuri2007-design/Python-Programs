def uppercase_decorator(original_function):
    def wrapper():
        message=original_function()
        print(message.upper())
    return wrapper
@uppercase_decorator
def greet():
    return "hello,yasaswini"
greet()
